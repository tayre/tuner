# String

A lightweight static starter page for a guitar tuner.

## Deploy to Cloudflare Pages

Create a Pages project from this repository and use these settings:

- **Root directory:** `tuner`
- **Build command:** *(leave blank)*
- **Build output directory:** `.`

The page is plain HTML, CSS, and JavaScript—no install or build step is needed.
